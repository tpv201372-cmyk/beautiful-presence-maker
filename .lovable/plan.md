

## Кейсы учеников: фото Дианы

### Что меняем в `src/components/landing/Cases.tsx`

1. **Добавить фото Дианы** (`user-uploads://IMAGE_2026-04-21_15_54_09.jpg`):
   - Скопировать в `src/assets/cases/diana.jpg`.
   - Импортировать в `Cases.tsx` как `dianaPhoto`.
   - Использовать одиночное фото (как у Ильвины) — поле `photo` в объекте кейса Дианы.

2. **Удалить плейсхолдер** с иероглифами 成功 — он больше не нужен, так как у всех трёх кейсов теперь есть изображения. Убираем третью ветку условного рендера, оставляем только `c.images` → before/after и `c.photo` → одиночное фото.

### Технические детали

- Файл: `src/components/landing/Cases.tsx`.
- Новый ассет: `src/assets/cases/diana.jpg` (копия из user-uploads).
- Стиль одиночного фото уже определён и используется для Ильвины — переиспользуем.
- Порядок кейсов остаётся прежним: Азамат → Ильвина → Диана.

