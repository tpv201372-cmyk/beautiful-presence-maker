

## Кейсы учеников: фото Ильвины + новый порядок

### Что меняем в `src/components/landing/Cases.tsx`

1. **Добавить фото Ильвины** (`user-uploads://IMAGE_2026-04-21_15_54_13.jpg`):
   - Скопировать в `src/assets/cases/ilvina.jpg`.
   - Импортировать в `Cases.tsx` как `ilvinaPhoto`.
   - В отличие от Азамата (у которого «было/стало» — два скриншота подписчиков), у Ильвины только одно фото — её портрет. Поэтому для неё используем **одиночное фото**, а не пару before/after.

2. **Расширить тип `CaseItem`** — добавить опциональное поле `photo?: string` для одиночного фото (когда нет before/after).

3. **Обновить рендер карточки** — добавить третью ветку:
   - если `c.images` → before/after (как сейчас, для Азамата);
   - иначе если `c.photo` → одиночное фото на всю ширину карточки (для Ильвины);
   - иначе → плейсхолдер с иероглифами 成功 (для Дианы).

4. **Поменять порядок** в массиве `cases`:
   1. Азамат
   2. Ильвина (с фото)
   3. Диана

### Технические детали

- Файл: `src/components/landing/Cases.tsx`.
- Новый ассет: `src/assets/cases/ilvina.jpg` (копия из user-uploads).
- Стиль одиночного фото: рамка `border border-navy/15`, `bg-cream`, `object-contain`, `max-h-[420px]` — визуально согласовано с блоком Азамата.
- Тексты «Было / Стало» под фото остаются без изменений во всех трёх карточках.

