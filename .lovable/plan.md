

## Мобильный слайдер отзывов: группировка коротких отзывов на один слайд

Эта работа уже была реализована в предыдущем сообщении — массив `mobileSlides` добавлен, мобильная карусель рендерит группы по 1–2 отзыва, десктопная сетка не тронута.

Если ты хочешь **переразбить группы по-другому** (например, добавить `guzel-course` в пару, или поменять порядок), скажи конкретно — и я обновлю только массив `mobileSlides` в `src/components/landing/Testimonials.tsx`.

Текущая раскладка в коде:

1. `anna` + `alena`
2. `leysan` + `adel`
3. `aliya` + `elena`
4. `guzel-course` (одиночно)
5. `evdokia` (одиночно)
6. `china-truck` (одиночно)
7. `first-parcel` (одиночно)
8. `kate` (одиночно)

Подтверди, что менять — или я ничего не трогаю.

